spring:
  application:
    name: curvekind-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/curvekind
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

    flyway:
      enabled: true

    jpa:
      hibernate:
        ddl-auto: validate
      open-in-view: false